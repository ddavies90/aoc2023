import "../env";
import { fetchPuzzleInput } from "../utils/fetchPuzzleInput";

const part1 = async () => {
  const input = await fetchPuzzleInput(0);
  return;
};

const part2 = () => {
  return;
};
